// Sistema de tiempo real simplificado
// El frontend usa polling automático cada 5 segundos
// No se requiere configuración adicional

export const notifyVotoRegistrado = async (payload = {}) => {
    // Función mantenida para compatibilidad pero no hace nada
    // El frontend actualiza automáticamente mediante polling
    return
}
